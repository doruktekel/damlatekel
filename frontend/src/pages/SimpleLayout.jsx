import React from "react";

const SimpleLayout = ({ children, theme }) => (
  <div>{children}</div>
  // <div className="grid  place-items-center  h-lvh">{children}</div>
);

export default SimpleLayout;
